mutation InitialAuthentication($ticket: String!) {
    initialAuthentication(
        ticket: $ticket
    ) {
        user {
            id
            netid
            firstName
            lastName
            role
        }
        token
    }
}
